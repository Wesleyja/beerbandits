<div
  id="map"
  style="width: 100%;
  height: 340px;"
  data-markers="<%= @markers.to_json %>"
  data-center="<%= Geocoder.search(params[:results][:location]).first.coordinates %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
>
</div>

<div class="swiper-container">
  <div class="swiper-wrapper">
    <% @final_results.first(5).each do |x| %>
      <div class="swiper-slide">
        <div class="product-card card">
          <img src="https://images.unsplash.com/photo-1558642891-54be180ea339?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=634&q=80" class="product-card-image card-img-top" alt="beer-image"/>
          <div class="product-card-details">
            <h2 class="product-card-brand"><%= x.first.product.drink.name %></h2>
            <h3 class="product-card-name"><%= x.first.inventory.store.name %></h3>
            <p class="product-card-price">$<%= x.first.inventory.price_cents.to_f/100 %>.  |   <%= x.first.product.drink.abv %>%</p>
            <div class="product-card-footer">
              <div class="product-card-distance"><%= (((590.4761905/5)*x.first.inventory.price_cents.to_f/100)/(x.last)).round %>m</div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <div class="swiper-pagination"></div>
</div>



<!-- <div>
  <% @final_results.first(5).each do |x| %>
  <div class="card">
    <h1><%= x.first.product.drink.name %></h1>
    <h3><%= x.first.product.drink.abv %>%</h3>
    <h3>$<%= x.first.inventory.price_cents.to_f/100 %></h3>
    <h3><%= x.first.inventory.store.name %></h3>
    <h3><%= (((590.4761905/5)*x.first.inventory.price_cents.to_f/100)/(x.last)).round %>m</h3>
  </div>
  <% end %>
</div> -->